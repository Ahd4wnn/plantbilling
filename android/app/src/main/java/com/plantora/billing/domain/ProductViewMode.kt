package com.plantora.billing.domain

/**
 * How the product catalogue is laid out. Blocks show a big photo and suit
 * recognising plants at a glance; the compact list fits roughly twice as many
 * items on screen, which shops with long catalogues asked for. Chosen per device
 * and shared by the Bill picker and the Products tab.
 */
enum class ProductViewMode { GRID, LIST }
